# FindMed profiles

Each clinic or practitioner has a dedicated directory. The directory contains its structured `profile.js`, verification notes, and later its approved images.

Add every new profile to `profiles/index.js` so it becomes available to the website.
